{
  "name": "afrovibe",
  "version": "1.0.0",
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "@openzeppelin/contracts-upgradeable": "^5.0.2"
    "@nomiclabs/hardhat-waffle": "^2.0.3",
    "@openzeppelin/contracts": "^4.9.3",
    "@onramp.money/onramp-web-sdk": "^1.0.0",
    "dotenv": "^16.0.3",
    "ethers": "^5.7.2",
    "graphql": "^16.6.0",
    "node-fetch": "^2.6.7",
    "react-native": "^0.71.0",
    "react-native-unity-webgl": "^2.0.0",
    "webrtc-adapter": "^8.1.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/hardhat-upgrades": "^3.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0"
    "chai": "^4.3.7",
    "mocha": "^10.2.0"
  },
  "name": "afrovibe-paymaster",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deployPaymaster.js --network sonic",
    "test": "hardhat test"
  }, 
  "scripts": {
    "start": "react-native start",
    "test": "mocha tests/",
    "deploy": "npx hardhat run scripts/deploy.js --network sonic"
  }
}
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deployPaymasterFunder.js --network sonic",
    "fund": "hardhat run scripts/fundPaymasterFunder.js --network sonic"
  }
}

"scripts": {
  "test": "jest"
},
"jest": {
  "testMatch": [
    "<rootDir>/src/components/__tests__/**/*.test.js",
    "<rootDir>/mobile/components/__tests__/**/*.test.js"
  ]
}
